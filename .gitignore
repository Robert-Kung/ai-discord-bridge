.env
# Real compose holds your private project list + absolute paths — keep it local.
# Public template is docker-compose.example.yml.
docker-compose.yml
__pycache__/
*.pyc
.venv/
.pytest_cache/
.DS_Store
# Internal planning doc — real name + A↔B debate notes; never publish.
RELEASE_PLAN.md
# Internal security/remediation planning — host infra topology, IPs, project
# list, A↔B notes. Same reason as RELEASE_PLAN.md: never publish.
docs/
# Local Claude Code tooling (opsx slash-commands/skills) — not part of this project.
.claude/
